package dev.elysium.combat.clazz;

/**
 * Enum cac class co trong game.
 * Them class moi: them vao day + them vao classes.yml + skills.yml
 */
public enum PlayerClass {
    NONE,
    WARRIOR,
    MAGE,
    RANGER,
    ASSASSIN,
    SUPPORT
}
